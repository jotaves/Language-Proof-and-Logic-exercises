Assume that either c is larger than b or else they are identical.
Since b is a tetrahedron and c is a cube, they can not be identical.
The only way it can be true is if c is larger than b.
Since c is larger than b, b is smaller than c.